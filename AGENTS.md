# Project AI Working Agreement

너는 이 저장소의 프로젝트 팀원이다.

## 프로젝트 목표

이 프로젝트는 멀티 계정 AWS 환경에서 세 가지 보안 시나리오를 공통 흐름으로 목업 검증하고, 그중 선정한 하나의 대표 시나리오를 테스트 AWS 환경에서 자동 대응까지 검증한다.

이벤트 수집
→ 위험도 판단
→ 승인 또는 대응 결정
→ 대응 결과 기록
→ 보고서 생성

대표 시나리오에는 필요에 따라 JIT 권한 발급·TTL 만료 또는 보안 설정 원복과 같은 실제 AWS 대응을 연결한다.

## 작업 시작 전

1. 저장소의 README.md와 관련 문서를 먼저 읽는다.
2. 현재 브랜치와 변경 사항을 확인한다.
3. 요청받은 Issue의 범위를 확인한다.
4. 관련된 기존 코드를 검색한 뒤 수정한다.
5. 요청받지 않은 구조 변경이나 대규모 리팩터링은 하지 않는다.
6. 모호한 요구사항은 임의로 확장하지 말고 합리적인 가정을 문서에 남긴다.

## 작업 단위

1. 하나의 Issue는 하나의 작은 목표를 가진다.
2. 하나의 Issue는 하나의 작업 브랜치에서 처리한다.
3. 작업이 완료되면 PR을 생성한다.
4. 하나의 PR에는 하나의 목적만 담는다.
5. 기능 구현, 테스트, 관련 문서를 같은 PR에서 처리한다.
6. 서로 무관한 변경 사항을 하나의 PR에 섞지 않는다.

## 브랜치 이름

다음 형식을 사용한다.

- feat/<간단한-기능명>
- fix/<간단한-문제명>
- test/<간단한-테스트명>
- docs/<간단한-문서명>
- chore/<간단한-설정명>

## 코드 작성 원칙

1. 기존 프로젝트 구조와 스타일을 먼저 따른다.
2. 필요한 최소한의 변경만 한다.
3. 중복 코드와 불필요한 추상화를 만들지 않는다.
4. 보안 관련 로직은 편의성보다 안전성을 우선한다.
5. IAM 권한은 필요한 리소스와 작업으로 제한한다.
6. API 키, AWS 자격증명, 비밀번호를 코드나 로그에 남기지 않는다.
7. 실제 AWS 리소스를 변경하는 코드는 영향 범위를 명확히 하고, 대표 시나리오 하나에 한정한다.
8. Mock과 실제 AWS 연동을 구분한다.
9. 공통 이벤트 형식은 docs/event-schema.md를 따른다.

## 테스트

변경 후 가능한 범위에서 다음을 실행한다.

```bash
ruff check .
ruff format --check .
pytest
```

다음 변경에는 반드시 테스트를 추가한다.

- 이벤트 파싱
- 위험도 판단
- 권한 발급·회수
- 공격 탐지
- 대응 플레이북
- 입력값 검증

테스트를 실행하지 못했다면 그 이유를 PR에 적는다.

## LLM 사용 규칙

1. LLM이 생성한 코드를 그대로 신뢰하지 않는다.
2. 생성된 코드의 동작과 보안 영향을 직접 확인한다.
3. AWS 권한과 외부 API 호출을 특히 검토한다.
4. 담당자는 제출한 코드를 설명할 수 있어야 한다.
5. PR에 LLM을 사용한 범위와 사람이 검증한 내용을 기록한다.

## PR 규칙

PR에는 다음을 포함한다.

- 변경 목적과 범위
- 관련 Issue
- 테스트 방법과 결과
- 보안 영향
- LLM 사용 범위
- 알려진 한계
- 리뷰어가 확인해야 할 부분

## 안전 규칙

1. `main` 브랜치에 직접 push하지 않는다.
2. API 키, AWS 자격증명, 비밀번호를 커밋하지 않는다.
3. 실제 공격은 테스트 계정과 더미 리소스에서만 수행한다.
4. 운영 AWS 리소스를 변경하는 작업은 사전에 영향 범위를 확인한다.
5. 테스트를 통과시키기 위해 테스트나 검증 로직을 삭제하지 않는다.
